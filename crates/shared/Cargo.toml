[package]
name = "shared"
version = "0.1.0"
edition = "2024"

[dependencies]
avian3d = {workspace=true, features = ["serialize"]}
bevy = { workspace = true }
lightyear = {workspace = true, features = ["trace", "client", "server"]}
serde = "1.0.228"
rand = { workspace = true }
hex = "0.4.3"
