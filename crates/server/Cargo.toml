[package]
name = "server"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.17.3"
bevy-inspector-egui = "0.35.0"
lightyear = {version = "0.25.5", features = ["server", "netcode", "replication", "udp", "trace"]}
lightyear_replication = "0.25.5"
shared = { version = "0.1.0", path = "../shared" }
